import React from 'react';

const ExamWaiting = ({ translate }) => (
  <div className="container">
    <div className="bio-graph-heading">
      {translate('exam.waiting.aciklama')}
    </div>
  </div>
);

export default ExamWaiting;
